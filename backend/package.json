{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Task manager",
  "main": "index.js",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=prod node index.js",
    "dev": "cross-env NODE_ENV=dev --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "install:dependencies": "npm install && cd ../frontend && npm install && cd ../backend",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy": "npm run build:ui && npm start",
    "assignment:start": "npm run install:dependencies && npm run deploy"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^10.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}